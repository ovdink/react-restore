import React from 'react';

import './Error-indicator.scss';

const ErrorIndicator = () => {
  return (
    <div>
      Error! Please reload the page! (try again and try, bcs its Math.random())
    </div>
  );
};

export default ErrorIndicator;
